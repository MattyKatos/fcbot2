<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>FCBot Dashboard</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
  <style>
    :root{ --bg:#0f172a; --card:#111827; --muted:#9ca3af; --fg:#e5e7eb; --primary:#06b6d4; }
    *{box-sizing:border-box}
    body{margin:0;background:linear-gradient(180deg,#0b1220 0%, #0f172a 100%);font-family:Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial;color:var(--fg)}
    header{display:flex;align-items:center;justify-content:space-between;padding:20px 28px;border-bottom:1px solid #1f2937;background:rgba(17,24,39,.6);backdrop-filter: blur(8px)}
    header .title{font-weight:800;letter-spacing:.5px}
    header .user{display:flex;align-items:center;gap:12px;color:var(--muted)}
    main{padding:28px;max-width:1100px;margin:0 auto}
    .grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:18px}
    .card{background:linear-gradient(180deg, rgba(255,255,255,.04),rgba(255,255,255,.02));border:1px solid rgba(255,255,255,.08);border-radius:14px;padding:16px 18px; text-align:center}
    .card h3{margin:0 0 6px;font-size:14px;color:var(--muted);font-weight:600;letter-spacing:.2px}
    .card .num{font-size:36px;font-weight:800;letter-spacing:.5px}
    footer{color:var(--muted);text-align:center;padding:18px 28px;border-top:1px solid #1f2937;margin-top:28px}
    a.btn{display:inline-block;padding:10px 14px;background:var(--primary);color:#06252c;border-radius:10px;text-decoration:none;font-weight:700}
  </style>
</head>
<body>
  <header>
    <div class="title">FCBot Dashboard</div>
    <div class="user">Logged in as <strong><%= user.global_name || user.username %></strong> · <a class="btn" href="/logout">Logout</a></div>
  </header>

  <main>
    <div class="grid">
      <div class="card"><h3>Users</h3><div class="num"><%= counts.users %></div><% if (isAdmin) { %><div style="margin-top:8px"><a class="btn" href="/users">Manage Users</a></div><% } %></div>
      <div class="card"><h3>Characters</h3><div class="num"><%= counts.characters_registered %> / <%= counts.characters %></div><% if (isAdmin) { %><div style="margin-top:8px"><a class="btn" href="/characters">Manage Characters</a></div><% } %></div>
      <div class="card"><h3>Members</h3><div class="num"><%= counts.members %></div><% if (isAdmin) { %><div style="margin-top:8px"><a class="btn" href="/members">Manage Members</a></div><% } %></div>
      <div class="card"><h3>Ranks</h3><div class="num"><%= counts.ranks %></div><% if (isAdmin) { %><div style="margin-top:8px"><a class="btn" href="/ranks">Manage Ranks</a></div><% } %></div>
      <div class="card"><h3>Roles</h3><div class="num"><%= counts.roles %></div><% if (isAdmin) { %><div style="margin-top:8px"><a class="btn" href="/roles">Manage Roles</a></div><% } %></div>
      <div class="card"><h3>Channels</h3><div class="num"><%= counts.channels %></div><% if (isAdmin) { %><div style="margin-top:8px"><a class="btn" href="/channels">Manage Channels</a></div><% } %></div>
      <div class="card"><h3>Logs</h3><div class="num"><%= counts.logs %></div><% if (isAdmin) { %><div style="margin-top:8px"><a class="btn" href="/logs">Logs Viewer</a></div><% } %></div>
    </div>
  </main>

  <footer>
    <div>Base URL: <%= baseUrl %></div>
  </footer>
</body>
</html>
