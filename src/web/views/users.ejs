<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Manage Users</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
  <style>
    :root{ --bg:#0f172a; --card:#111827; --muted:#9ca3af; --fg:#e5e7eb; --primary:#06b6d4; }
    *{box-sizing:border-box}
    body{margin:0;background:linear-gradient(180deg,#0b1220 0%, #0f172a 100%);font-family:Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial;color:var(--fg)}
    header{display:flex;align-items:center;justify-content:space-between;padding:20px 28px;border-bottom:1px solid #1f2937;background:rgba(17,24,39,.6);backdrop-filter: blur(8px)}
    header a{color:var(--fg);text-decoration:none}
    main{padding:28px;max-width:1100px;margin:0 auto}
    .card{background:linear-gradient(180deg, rgba(255,255,255,.04),rgba(255,255,255,.02));border:1px solid rgba(255,255,255,.08);border-radius:14px;padding:16px 18px}
    table{width:100%;border-collapse:collapse}
    th,td{padding:10px;border-bottom:1px solid rgba(255,255,255,.08)}
    input[type=number]{width:120px;padding:8px 10px;border-radius:10px;border:1px solid rgba(255,255,255,.15);background:#0b1220;color:var(--fg)}
    .btn{padding:8px 12px;border-radius:10px;border:1px solid rgba(255,255,255,.15);background:#0b1220;color:var(--fg);text-decoration:none}
    .btn.primary{background:var(--primary);color:#06252c;border:0;font-weight:800}
    .notice{margin:10px 0;color:#7dd3fc}
    .danger{background:#dc2626;color:#fff;border:0}
  </style>
</head>
<body>
  <header>
    <div>FCBot — Manage Users</div>
    <nav>
      <a class="btn" href="/dashboard">Dashboard</a>
    </nav>
  </header>

  <main>
    <% if (notice) { %><div class="notice"><%= notice %></div><% } %>
    <div class="card">
      <table>
        <thead>
          <tr>
            <th>User</th>
            <th>Discord</th>
            <th>Characters</th>
            <th>Currency</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% for (const u of users) { %>
            <tr>
              <td><strong><%= u.display_name || u.user_name %></strong> <span class="muted">(admin: <%= u.is_admin ? 'yes' : 'no' %>)</span></td>
              <td><a class="btn" href="https://discord.com/users/<%= u.user_discord_id %>" target="_blank"><%= u.user_discord_id %></a></td>
              <td><%= u.char_count %></td>
              <td><%= u.currency %></td>
              <td>
                <form method="post" action="/users/currency" style="display:inline-block;margin-right:4px">
                  <input type="hidden" name="user_id" value="<%= u.user_id %>" />
                  <input type="number" name="amount" placeholder="amount" />
                  <button class="btn primary" type="submit" name="op" value="add">Add</button>
                  <button class="btn" type="submit" name="op" value="remove">Remove</button>
                </form>
                <form method="post" action="/users/delete" style="display:inline-block" onsubmit="return confirm('Delete this user? This will cascade linked data.');">
                  <input type="hidden" name="discord_id" value="<%= u.user_discord_id %>" />
                  <button class="btn danger" type="submit">Delete</button>
                </form>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>
</body>
</html>
